#include <stdio.h>

int main() 
{
    int N, M;

    scanf("%d%d", &N, &M);

    int arr[N];

    for (int i = 0; i < N; i++)
        arr[i] = i;

    for (int i = 0; i < M; i++)
    {
        int r1, r2;
        int tmp[N];

        scanf("%d%d", &r1, &r2);        

        for (int j = r1; r1 < r2; j++)
        {
            //피곤해잘랭    
        }

    }
}